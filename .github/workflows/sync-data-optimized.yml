name: Sync Data to koungs-day-web (Optimized)

on:
  workflow_run:
    # Quota, Subsidies 스크래퍼 완료 후 실행
    workflows: ["EV Quota Scraper", "EV Subsidy Scraper"]
    types: [completed]

  # 수동 테스트용
  workflow_dispatch:

jobs:
  sync-data:
    runs-on: ubuntu-latest

    steps:
      # ========================================
      # 1. 스크래퍼 리포 체크아웃
      # ========================================
      - name: Checkout scraper repository
        uses: actions/checkout@v4
        with:
          path: scraper

      # ========================================
      # 2. 웹 리포 체크아웃
      # ========================================
      - name: Checkout koungs-day-web repository
        uses: actions/checkout@v4
        with:
          repository: koungsday/koungs-day-web
          token: ${{ secrets.PAT_TOKEN }}
          path: web

      # ========================================
      # 3. 데이터 파일 복사
      # ========================================
      - name: Copy scraped data to web repo
        run: |
          echo "📥 Copying scraped data..."

          # public/data 폴더 확인/생성
          mkdir -p web/public/data

          # 정규화된 데이터 파일 복사
          cp scraper/data/quota.json web/public/data/ || echo "⚠️ quota.json not found"
          cp scraper/data/subsidies.json web/public/data/ || echo "⚠️ subsidies.json not found"
          cp scraper/data/vehicles.json web/public/data/ || echo "⚠️ vehicles.json not found"

          # 복사된 파일 확인
          echo "📁 Files in web/public/data/:"
          ls -lh web/public/data/

      # ========================================
      # 4. 변경사항 감지 (핵심!)
      # ========================================
      - name: Check if data actually changed
        id: check_changes
        working-directory: web
        run: |
          # Git diff로 실제 변경 확인
          if git diff --quiet public/data/quota.json public/data/subsidies.json public/data/vehicles.json 2>/dev/null; then
            # 변경 없음
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No data changes detected"
            echo "💡 Skipping commit to save Vercel deployment!"
          else
            # 변경 있음
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ Data changes detected:"

            # 어떤 파일이 변경되었는지 로그
            if ! git diff --quiet public/data/quota.json 2>/dev/null; then
              echo "  📝 quota.json changed"
              git diff --stat public/data/quota.json
            fi

            if ! git diff --quiet public/data/subsidies.json 2>/dev/null; then
              echo "  📝 subsidies.json changed"
              git diff --stat public/data/subsidies.json
            fi

            if ! git diff --quiet public/data/vehicles.json 2>/dev/null; then
              echo "  📝 vehicles.json changed"
              git diff --stat public/data/vehicles.json
            fi
          fi

      # ========================================
      # 5. 조건부 커밋 (변경 있을 때만!)
      # ========================================
      - name: Commit and push ONLY if data changed
        if: steps.check_changes.outputs.changed == 'true'
        working-directory: web
        run: |
          echo "📤 Committing changes to koungs-day-web..."

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          # 변경된 파일만 add
          git add public/data/quota.json public/data/subsidies.json public/data/vehicles.json

          # 커밋 메시지 생성
          git commit -m "chore: Auto-update scraper data

          Updated at: $(date -u +'%Y-%m-%d %H:%M:%S UTC')

          Data sources:
            - Quota: 무공해차 누리집 (할당량)
            - Vehicles: 차량 마스터 (국고 보조금)
            - Subsidies: 지역별 지자체 보조금 (정규화)

          Synced from koungsday/longrange-scraper @ ${{ github.sha }}"

          # Push
          git push

          echo "✅ Data synced and deployed to Vercel"

      # ========================================
      # 5.5. Redis 일별 스냅샷 저장
      # ========================================
      - name: Save daily quota snapshots to Redis
        if: steps.check_changes.outputs.changed == 'true'
        continue-on-error: true
        run: |
          echo "📊 Saving daily quota snapshots to Redis..."

          # Secrets 검증
          if [ -z "${{ secrets.KOUNGS_DAY_WEB_URL }}" ]; then
            echo "⚠️ KOUNGS_DAY_WEB_URL secret not configured"
            echo "   Please set it in repository Settings > Secrets and variables > Actions"
            echo "   Example: https://your-web-app.vercel.app"
            exit 0
          fi

          if [ -z "${{ secrets.SYNC_SECRET }}" ]; then
            echo "⚠️ SYNC_SECRET secret not configured"
            echo "   Please set it in repository Settings > Secrets and variables > Actions"
            exit 0
          fi

          echo "✓ Secrets configured, proceeding with API call..."

          # API 호출 (에러 발생 시에도 계속 진행)
          set +e
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            "${{ secrets.KOUNGS_DAY_WEB_URL }}/api/quota/sync" \
            -H "Authorization: Bearer ${{ secrets.SYNC_SECRET }}" \
            -H "Content-Type: application/json" \
            --max-time 30 2>&1)
          CURL_EXIT_CODE=$?
          set -e

          # curl 명령 자체 실패 확인
          if [ $CURL_EXIT_CODE -ne 0 ]; then
            echo "⚠️ curl command failed (exit code: $CURL_EXIT_CODE)"
            case $CURL_EXIT_CODE in
              3)
                echo "   Malformed URL - check KOUNGS_DAY_WEB_URL format"
                echo "   Current URL pattern: ${{ secrets.KOUNGS_DAY_WEB_URL }}/api/quota/sync"
                ;;
              6)
                echo "   Could not resolve host - DNS issue or wrong URL"
                ;;
              7)
                echo "   Failed to connect to host"
                ;;
              28)
                echo "   Timeout after 30 seconds"
                ;;
              *)
                echo "   See curl exit codes: https://curl.se/docs/manpage.html#EXIT"
                ;;
            esac
            echo "   Response: $RESPONSE"
            exit 0
          fi

          # HTTP 응답 코드 확인
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')

          echo "Response: $BODY"

          if [ "$HTTP_CODE" != "200" ]; then
            echo "⚠️ Snapshot sync failed (HTTP $HTTP_CODE), but continuing..."
            echo "   This won't block data sync. Snapshots will be saved next time."
          else
            echo "✅ Daily snapshots saved successfully"
            echo "   Data will be available for trend charts"
          fi

      # ========================================
      # 6. 결과 로그
      # ========================================
      - name: Log sync result
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          if [ "${{ steps.check_changes.outputs.changed }}" == "true" ]; then
            echo "✅ Sync completed: Data changed and committed"
            echo "🚀 Vercel deployment triggered"
          else
            echo "⏭️  Sync skipped: No data changes"
            echo "💰 Saved 1 Vercel deployment!"
          fi
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
